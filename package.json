{
  "name": "ppp-info",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:phase-lock": "tsx tests/phase-locked-stereoscopy.test.ts",
    "test:adapters": "tsx tests/adapter.test.ts",
    "test:bridge": "tsx tests/adapter-bridge.test.ts",
    "test:all": "npm run test && npm run test:phase-lock && npm run test:adapters && npm run test:bridge"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
