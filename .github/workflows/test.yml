name: Test PPP v3

on:
  push:
    branches: [main, 'claude/**']
  pull_request:
    branches: [main]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cra-pom-v2

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: cra-pom-v2/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

      - name: TypeScript check
        run: npm run typecheck

      - name: Build
        run: npm run build

  e2e-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cra-pom-v2

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: cra-pom-v2/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run Gemini E2E tests
        if: ${{ secrets.GEMINI_API_KEY != '' }}
        run: npm run test:e2e:gemini
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Run Voyage E2E tests
        if: ${{ secrets.VOYAGE_API_KEY != '' }}
        run: npm run test:e2e:voyage
        env:
          VOYAGE_API_KEY: ${{ secrets.VOYAGE_API_KEY }}
